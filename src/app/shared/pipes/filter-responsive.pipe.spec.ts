import { FilterResponsivePipe } from './filter-responsive.pipe';

describe('FilterResponsivePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResponsivePipe();
    expect(pipe).toBeTruthy();
  });
});
