import { ComponentInputsPipe } from './component-inputs.pipe';

describe('ComponentInputsPipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentInputsPipe();
    expect(pipe).toBeTruthy();
  });
});
