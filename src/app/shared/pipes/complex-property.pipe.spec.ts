import { ComplexPropertyPipe } from './complex-property.pipe';

describe('ComplexPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplexPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
