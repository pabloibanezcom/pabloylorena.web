import { AdjustHeightDirective } from './adjust-height.directive';

describe('AdjustHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new AdjustHeightDirective();
    expect(directive).toBeTruthy();
  });
});
